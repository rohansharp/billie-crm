# Core dependencies
redis>=5.0
motor>=3.3
pydantic[email]>=2.0
pydantic-settings>=2.0
structlog>=24.0
email-validator>=2.0

# Billie Event SDKs
# Install event sdks directly from git repo
git+https://${GITHUB_TOKEN}@github.com/BillieLoans/billie-event-sdks.git@accounts-v2.5.0#subdirectory=packages/accounts
git+https://${GITHUB_TOKEN}@github.com/BillieLoans/billie-event-sdks.git@customers-v2.0.0#subdirectory=packages/customers
git+https://${GITHUB_TOKEN}@github.com/BillieLoans/billie-event-sdks.git@ledger-v1.0.0#subdirectory=packages/ledger


# Development dependencies (optional)
pytest>=8.0
pytest-asyncio>=0.23

